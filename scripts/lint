#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

source "./scripts/_lib.sh"

run_check() {
  # Add web lint if available
  # cd apps/web && pnpm lint
  echo "SKIP: Lint not strictly configured yet"
}

ai_team_run_logged "lint" run_check
