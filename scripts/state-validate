#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
cd "$ROOT_DIR"

source "./scripts/_lib.sh"

run_check() {
  python3 -m jsonschema -i docs/ops/ai-team-state.json docs/ops/schema/ai-team-state.schema.json
}

ai_team_run_logged "state-validate" run_check
